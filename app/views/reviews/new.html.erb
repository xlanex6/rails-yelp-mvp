
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content%>
  <%= f.input :rating, collection: 1..5, as: :radio_buttons, :class => 'inline_label' %>
  <%= f.button :submit%>
<% end %>


input_html: { class: 'special' }
html: { class: 'form-inline' }
